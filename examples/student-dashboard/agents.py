# Student Dashboard example — backend agent declarations.
# Only smart widgets with agent: { id, promptFile, toolsModule } need entries here.
# This example has only dumb widgets — no backend agents needed.

WIDGET_AGENTS = []
