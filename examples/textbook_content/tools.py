"""Textbook Content — standalone backend tools.

Spawn tools live inside each widget's config.py (self-contained).
This file only holds standalone orchestrator tools (MCP queries, etc.)
that are NOT tied to any specific widget.

Currently empty — each widget is fully self-contained.
"""

# No standalone tools — spawn tools are per-widget in config.py
all_tools = []
